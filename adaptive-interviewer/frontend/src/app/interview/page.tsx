// Interview chat page — chunk G wires real state machine.
// Stub placeholder so the route resolves after scaffold.
export default function InterviewPage() {
  return (
    <main className="mx-auto max-w-2xl px-6 py-20 text-neutral-300">
      <div className="font-mono text-xs uppercase tracking-widest text-darpan-lime">
        Interview
      </div>
      <h1 className="mt-2 text-3xl font-semibold">Coming online in chunk G.</h1>
      <p className="mt-4 text-neutral-500">
        Backend state machine runs end-to-end (chunks B–F); the chat UI is
        wired last.
      </p>
    </main>
  );
}
